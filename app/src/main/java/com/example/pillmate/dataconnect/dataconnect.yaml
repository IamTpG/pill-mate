# dataconnect.yaml
specVersion: "v1alpha"
serviceId: "pillmate"
location: "us-central1"
schema:
  source: "."
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "pillmate-instance"
connectorDirs:
  - "."